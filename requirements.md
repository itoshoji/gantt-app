# ガントチャート型タスク管理アプリ 要件定義書 (v3)

## 1. 背景・目的
Excelで手作業管理していた月次ガント表（定例業務・プロジェクトの進捗管理）を、マウス操作中心の
インタラクティブなWebアプリに置き換える。将来的には仕事だけでなくプライベートのタスク管理にも
使えるようにする。

## 2. 対象ユーザー
- 本人（1名）のみ。複数ユーザーでの共有・招待機能は持たない。
- 用途として、画面を人に見せながら現在の業務状況を説明する場面を想定する（UIは見やすさ・
  プレゼン適性も意識する）。

## 3. 用語定義
| 用語 | 意味 |
|---|---|
| 項目 | 定例業務またはプロジェクトの単位（例:「マニフェスト精算」）。タグ（定例／プロジェクト）と色を持つ。※v1では「大枠」と呼んでいたもの |
| 中タスク | 項目の下にぶら下がる、月単位で扱う作業のまとまり。ガントバーとして表示される |
| 小タスク | 中タスクの下にぶら下がる、日単位で期間を持つ具体的な作業 |
| タグ | 項目に付与する種別。「定例」（暖色系）と「プロジェクト」（寒色系）の2種類 |

## 4. 機能要件

### 4.1 一覧ページ（メイン画面）
- 画面上部のトグルで **仕事 / プライベート** を切り替える（選択状態は次回起動時も保持）。
  項目はどちらか一方に属し、選択中の側の項目だけをガント・カレンダー・日ビューに表示する。
  仕事＝青、プライベート＝緑でトグルを塗り分け、今どちらを見ているか一目で分かるようにする
  （業務状況を人に見せる場面でプライベートの予定が映り込まないようにするため）。
  項目の右クリックメニューから所属を移動できる。新規項目は選択中の側に作られる。
- 画面上部のトグルで **年 / 四半期 / 日** の3つの表示を切り替える（選択状態は次回起動時も保持）。
- 左列に項目（タグ＋名前）、右側に4月〜3月の12ヶ月が横並びのガントエリアを表示する（年表示）。
- 画面上部の「＋」ボタンから項目を新規作成する。作成時にタグ（定例／プロジェクト）を選び、
  対応する暖色系／寒色系パレットから具体的な色を選択する。
- 各項目の行内、空いている月のセルをクリックすると、その場で中タスクが新規作成され、
  そのまま名前を入力できる。中タスクは中身（小タスク）が空でも存在してよい。
- 中タスクはガントバーとして表示され、バー端をドラッグして期間（月単位）を伸縮、
  バー本体をドラッグして左右（月）に移動できる。
- 中タスクの色は項目の色を継承し、内包する小タスクの数が多いほど濃い色になる。
- 一覧ページの月部分（列ヘッダー）をクリックすると、その月のカレンダーをモーダル表示する
  （画面遷移なし、表示速度・テンポを重視する）。
- 各項目の**一番左にチェック欄**を持つ。普段は隠しておき、行にマウスを乗せたときと、
  チェック済みのときだけ表示する（左端が混んで項目名が痩せるのを避けるため）。
- チェックを入れると、一覧の**外側・上部の帯**に「◯件を選択中」「詳細を見る」
  「選択を解除」が出る。帯の高さは常に確保し、出入りで表が上下に動かないようにする。
  「詳細を見る」で、選んだ項目の**詳細ビュー**（4.7）を開く。

#### 四半期表示
- 表示する月を3ヶ月（第1=4-6月 / 第2=7-9月 / 第3=10-12月 / 第4=1-3月）に絞る。
  レイアウト・操作は年表示と同じ。
- 表示範囲からはみ出す中タスクは、バーの端を角ばらせたうえで
  「◯月から」「◯月まで」と表記する。

#### 日表示
- ガントではなく、直近の小タスクを時系列に並べたToDoリストを表示する。
- 「進行中」（開始済みでまだ終わっていないもの）と「これからの予定」（日付ごとの見出し付き）に分ける。
- 各行に、項目の色・小タスク名・「項目 › 中タスク」・期間を表示する。行をクリックすると
  その月のカレンダーを、その中タスクに絞って開く。
- 終了日が過ぎたものと、非表示の項目のものは出さない。

### 4.2 項目の操作
- **並び順**: 基本は作成した順。項目の左端のグリップをドラッグ＆ドロップして手動で並べ替えできる。
- **非表示**: 項目ごとに非表示ボタン（目のアイコン）を持つ。非表示にすると
  ガント上では色が薄くなり、カレンダーにはその項目の小タスクが表示されなくなる
  （ガント表からは消えず、薄く残る）。
- 名前をクリックするとその場で改名できる。
- **色は赤・青の2色のみ**。項目は「種類の区別が一目で付く」ことだけを担い、
  細かい色分けは中タスク側で行う。
- **その項目に属する小タスクを全てカレンダーに表示する**操作を持つ。
  以前は行にカレンダーアイコンを置いていたが、チェック欄に場所を譲って**廃止**し、
  右クリックメニューの「カレンダーで見る」に一本化した。
- 項目の追加ボタン（＋）は**項目一覧の一番下**に置く（追加位置と結果が視線上つながるため）。

### 4.2.1 クリック操作の体系
- **ワンクリック** … 選択（セル・中タスク・小タスクが対象。コピー／切り取りの的になる）
- **ダブルクリック** … 空のセルなら中タスクを新規作成、中タスクなら小タスク一覧を開く、
  カレンダーの空きマスなら小タスクを新規作成
- スマートフォンでのタッチ操作（長押しでの選択など）は**スマホ版を作るときに検討する**。
  現時点ではPCのマウス操作を前提とする。

### 4.2.2 コピー・切り取り・貼り付け・元に戻す
- 右クリックメニューとキーボードショートカットの両方から使える。
- 中タスクをコピー／切り取りすると**配下の小タスクも一緒に**運ばれる。
  貼り付け先では月だけが変わり、小タスクの日付も同じ月数だけずれる。
- ショートカットは **Cmd と Ctrl の両方**を受け付ける（Mac／Windowsのどちらの癖でも動く）。
  - コピー: Cmd/Ctrl + C ／ 切り取り: Cmd/Ctrl + X ／ 貼り付け: Cmd/Ctrl + V
  - 元に戻す: Cmd/Ctrl + Z
- **複製ドラッグだけはOSごとに変える**。Macは **Option(⌥) + ドラッグ**、Windowsは
  **Ctrl + ドラッグ**。Macでは Ctrl+クリックが右クリックそのものなので、
  Ctrl+ドラッグを複製に使うとコンテキストメニューと衝突するため。
- **元に戻す**はキーボードだけでなく**右クリックメニューにも置く**（マウスだけで戻せるように）。
  直前の操作の名前をメニューに表示する（例: 「元に戻す（削除）」）。

### 4.3 中タスクの操作
- 中タスクを**ダブルクリック**すると吹き出し（ポップオーバー）が表示され、内包する小タスクの
  一覧を確認・編集できる。
- **色を変更できる**（右クリック →「色を変更」）。**20色の一覧から1クリックで選ぶ**
  （OSのカラーピッカーは選ぶまでの手数が多いため採用しない）。
  「項目の色に戻す」も用意する。色を設定していない中タスクは親の項目の色を使う。
- **選択中の中タスクは Delete / Backspace キーで削除できる**（確認なし。戻すのは Cmd+Z）。
- 吹き出しにカレンダーアイコンを持ち、押すと**その中タスクの小タスクだけ**をカレンダーに表示する。
- **中タスクを月移動したとき、内包する小タスクも同じ月数だけ移動する**（日は維持。
  移動先の月に同じ日が存在しない場合は、その月の末日に丸める。例: 31日 → 30日／29日）。
- **中タスクを伸縮したとき**、期間からはみ出した小タスクだけを、前後それぞれ近い側の
  端の月にまとめる（範囲内の小タスクは動かさない）。

### 4.4 小タスクの操作
- 小タスクの色は所属する中タスクと同色（中タスクに色がなければ項目の色）。
- 期限（開始日・終了日）は、カレンダー上をドラッグして範囲選択することで設定する
  （キーボードでの日付入力は行わない）。
- 一覧の各行の右端に**✕ボタン**を持ち、**ワンクリックで即削除**する（確認ダイアログなし）。
  誤操作は「元に戻す」で復帰できるため確認は挟まない。
- **月をまたぐ小タスクを作成・表示・操作できる**（例: 12/28〜1/4 の年末年始）。
  カレンダーは前後の月にはみ出したマスも本物の日付として扱い、月境界を越えた
  ドラッグ・伸縮・範囲選択ができる。
- カレンダー上では、中タスクのガントバーと同じように**日をまたいでひと繋がりのバー**として描く。
  週や月をまたいで続く側は角を落として、続いていることが分かるようにする。
- **カレンダー上のバーを直接操作して予定を変更できる**:
  - バー本体をドラッグ → 期間の長さを保ったまま前後に移動
  - バーの左右の端をドラッグ → 開始日・終了日を伸縮
- 小タスク一覧は日付順に自動ソートされる（手動並び替えUIは持たない）。

### 4.5 削除・コンテキストメニュー
- 項目／中タスク／小タスクのいずれも、**右クリックで小窓（コンテキストメニュー）を開く**
  （Windowsのフォルダを右クリックしたときのような挙動）。メニューから操作を選ぶこと自体が
  確認の役割を果たすため、追加の確認ダイアログは出さない。
- メニュー項目（どのメニューにも末尾に「元に戻す」が付く）:
  | 対象 | 項目 |
  |---|---|
  | 項目 | 名前を変更 / カレンダーで見る / 色を変更（赤⇄青） / 表示・非表示の切り替え / 仕事・プライベート間の移動 / 削除 |
  | 中タスク | 名前を変更 / 小タスクを開く / カレンダーで見る / 色を変更 / コピー / 切り取り / 貼り付け / 削除 |
  | 小タスク | 名前を変更 / カレンダーで見る / コピー / 切り取り / 貼り付け / 削除 |
  | セル（空欄） | ここに中タスクを作る / 貼り付け |
  | カレンダーの空きマス | ここに予定を追加 / 貼り付け |

### 4.6 月間カレンダー
- 月ヘッダーから開いた場合は、**その月に入っている全ての小タスク**を表示する
  （非表示の項目のものを除く）。
- 中タスクの吹き出しから開いた場合は、**その中タスクの小タスクだけ**を表示する。
- 項目のカレンダーアイコンから開いた場合は、**その項目に属する小タスクを全て**表示する。
- **どの開き方でも、対象外の予定を消さずに薄く表示する**（絞り込みではなく強調）。
  予定を決めるときは他の予定との兼ね合いを見たいため。
- 予定バーは**2段組み**にし、小タスク名（濃い）の上に「項目 / 中タスク」（薄い）を添える。
  予定が増えたとき「これは何の仕事か」が一目で分かるようにするため。
- カレンダーは**画面いっぱいに近い大きさ**で開く（バーの情報量が増えたため幅が要る）。
- 空いているマスを**ダブルクリック**すると、その日に小タスクを新規作成できる
  （どの中タスクに入れるか決まる開き方――中タスクまたは項目から開いた場合――に限る）。
- **前後の月のマスも操作できる**。月境界をまたぐ予定はそのまま繋がったバーとして描画し、
  翌月に移動しても続きとして表示される。
- 前月・翌月へ移動できる。
- 予定が重なった場合は週ごとに段を詰めて並べる（前の週から続く予定ほど上の段に来る）。
  縦に長くなっても画面からはみ出さないよう、日付部分だけをスクロールさせ、
  曜日の見出しは上に固定する。
- **土曜はうっすら水色、日曜と祝日はうっすら赤色**にマスの背景を塗る（予定として置くのではなく
  背景だけで表現する）。祝日はネット接続なしで計算する（春分・秋分の近似式、ハッピーマンデー、
  振替休日、国民の休日に対応）。祝日が土曜と重なった場合は祝日（赤）を優先する。

### 4.7 詳細ビュー（少数の項目を深く設計する）
一覧が「項目×月で全体を俯瞰する場」なのに対し、詳細ビューは**1つまたは少数の項目を取り出して、
中身を組み立てる場**とする。一覧のチェック欄で選び、「詳細を見る」で開く。

- **画面いっぱいのモーダル**で開く（カレンダーと同じ土台。Esc または × で一覧に戻る）。
- カレンダー形式ではなく**ガントチャート**で表示する。
- 上部のトグルで **年 / 四半期 / 月 / 週** を切り替える（選択状態は次回起動時も保持）。
  トグルは表示範囲であると同時に、**横軸の目盛りの単位**を切り替える:
  | 表示 | 横軸 | 用途 |
  |---|---|---|
  | 年 / 四半期 | 月のマス | 中タスクの期間を決める |
  | 月 / 週 | 日のマス | 小タスクを組む |
- **縦の並びは「中タスクごとのブロック」**。ブロックの上に中タスクのバーを置き、その真下に、
  中タスクの期間だけを**点線で囲った縄張り**を作って、その中に小タスクを並べる。
  中タスクをまとめて最上段に並べる案も検討したが、期間が重なると点線が交差して読めなくなるため
  ブロックを縦に積む形とした。
- 複数の項目を選んだ場合は、項目の見出しを挟んで縦に積む。
- **小タスクは「月」「週」のときだけ表示する**。年・四半期では日単位が細かすぎて潰れるため出さない。
- **中タスクの期間の変更（移動・伸縮）は「年」「四半期」のときだけ**行える。
  中タスクは月単位のものなので、日の目盛りの上では期間を示す屋根として置くだけにする。
- 小タスクは日単位で、バー本体のドラッグで移動、端のドラッグで伸縮できる。
- 中タスクの期間内の空きマスを**ダブルクリック**すると、その日に小タスクを新規作成する。
  期間の外は小タスクを置く場所ではないので反応させない。
- **操作体系は一覧と揃える**。ワンクリック＝選択、右クリック＝メニュー、色の変更、
  コピー・切り取り・貼り付け、複製ドラッグ（Mac=⌥ / Windows=Ctrl）、元に戻す。
  選択状態は一覧・カレンダーと共有する。
- 土曜はうっすら水色、日曜と祝日はうっすら赤色に列の背景を塗る（カレンダーと同じ扱い）。

### 4.8 データ一括登録（運用でカバー、アプリ機能としては実装しない）
- 紙の予定表など（例: 年度末に配布される次年度の会議予定表）をチャットでClaude Codeに
  渡し、DBへの一括登録を都度依頼する運用とする。アプリ内にAI取り込み機能は作らない。

## 5. 非機能要件
- **操作性**: 文字入力（名前など）以外はマウス操作（クリック・ドラッグ・右クリック）で完結させる。
- **認証**: 当初はログインを設ける方針だったが、**本人判断により現時点では実装しない**
  （2026-07-27）。手軽さを優先した結果、URLと接続キーを知った人はデータを読み書きできる
  状態であることを承知のうえでの判断。デプロイ前に再検討する（詳細は CLAUDE.md）。
- **対応環境**: PC（会社PC・自宅PC）のブラウザ。スマートフォン対応は将来のスコープとし、
  今回は対象外（ただしDBは共有できるように設計しておく）。
- **パフォーマンス**: 月クリック→カレンダー表示など、操作に対するレスポンスの速さ・
  テンポ感を重視する。
- **可用性**: 個人利用のため高い可用性は求めないが、クラウドDB側の標準的なバックアップに任せる。

## 6. データモデル（概要）
```
項目 (id, name, tag[定例|プロジェクト], color[赤|青], scope[work|private], hidden, sort_order)
  └ 中タスク (id, 項目id, 年度, name, start_month, end_month, color[任意])
       └ 小タスク (id, 中タスクid, name, start_date, end_date)
```
中タスクの `color` は未設定（NULL）なら親の項目の色を使う。

## 7. 画面一覧
1. 一覧ページ（メイン画面） — 項目×12ヶ月のガントビュー
2. 月間カレンダー（モーダル） — 月ヘッダー / 中タスクの吹き出し / 項目の右クリックメニュー から表示
3. 詳細ビュー（モーダル） — 項目のチェック欄 →「詳細を見る」から表示（4.7）
4. ログイン画面 — 現時点では設けない（5. 非機能要件を参照）

## 8. スコープ外（v1ではやらない）
- スマートフォン専用UI
- 通知・リマインダー機能（期限接近メール等） → v2で検討
- AI一括登録のアプリ内蔵化（当面はチャットでの手動依頼運用）
- 複数ユーザーでの共有・招待機能
- 添付ファイルの添付
- レポート出力・進捗率集計などの分析機能

## 9. 制約条件
- 予算: 0円想定（Supabase無料枠、ホスティングは無料枠のサービスを利用）
- 期限: 特になし
- 開発体制: 実装はClaude Codeが主導し、ユーザーはアカウント作成など本人にしかできない
  操作と、動作確認・フィードバックを担当する

## 10. 技術構成（暫定）
開発機に Node.js / Homebrew / GitHub CLI が未インストールのため、ビルド環境の構築を
最小限にする方針とする。
- フロントエンド: ビルド不要のプレーンHTML / CSS / JavaScript
- データベース・認証: Supabase（JS SDKをCDN経由で読み込み、npm/ビルド不要で利用）
  - 現状はブラウザの localStorage に保存している。`store.js` を差し替えることで移行する
- ホスティング: Vercel または Netlify（GitHubリポジトリ連携によるデプロイ）
- バージョン管理: git（ローカルにインストール済み、確認済み）

## 11. 確認済み事項
- 会社PCから vercel.app / supabase.co / github.com へのアクセスは問題なし（確認済み）
