# gantt-app（スケジュール管理）

ガントチャート型タスク管理Webアプリ。Excelで手作業管理していた月次ガント表を置き換えるのが目的。
利用者は本人ひとり。

| 読むもの | 中身 |
|---|---|
| このファイル | 決まりごと・手順・設計。**作業前に必ず読む** |
| `requirements.md` | 仕様の正本（v3）。**作業前に必ず読む** |
| `docs/経緯.md` | 「なぜこうなっているか」の記録。疑問が湧いたときだけ |

---

## ⚠ 壊してはいけないもの

**ここを外すと動かなくなるか、穴が開く。**

- **RLSは `FOR ALL TO authenticated`。** リポジトリはPublicなので、守っているのは
  接続キーの秘匿ではなくDB側のRLS。**`allow all` に戻すと即座に穴が開く。**
  新しくテーブルを足したときも、RLSを有効にして同じポリシーを付けること
- **新規サインアップは停止済み**（`disable_signup: true`）。**再び有効にしないこと**
- **ES Modules は使わない。** `file://` で開くとCORSで動かなくなる。すべてクラシック
  スクリプトで、読み込み順は **config → holidays → auth → store → app**
  （後ろが前に依存している）
- **コードのファイルは階層を作らず平置きのまま。** `index.html` がファイル名で直接
  読み込んでいて、`build_standalone.py` もそれ前提。動かすとダブルクリック起動が壊れる
- **`vercel.json` の `Cache-Control: max-age=0, must-revalidate` を外さない。**
  ファイル名にハッシュが付かない構成なので、外すと古いJS/CSSが使われ続ける
- **`start_month` / `end_month` は TEXT カラム。** DBからは `"3"` のような文字列で
  返るが、app.js は月を0〜11の**数値**として大小比較する。読み込み時に `Number()` で
  戻すこと（`toTask()` で実施済み）。外すとバーの位置がずれる
- **新しい更新操作は `History.act(ラベル, fn)` で包む。** 包み忘れるとUndoで戻せなくなる
- **中タスクのドラッグは `startTaskDrag()` 1つだけ。** 一覧と詳細ビューは「1マスの幅」と
  「バーの置き直し方」を渡すだけの薄い包み（`startBarDrag` / `dvStartTaskDrag`）。
  **画面ごとにコピーして書かないこと。** 以前は76行と73行のほぼ同じ関数が並んでいて、
  片方だけ直す事故が起きる状態だった
- **詳細ビューを開いている間の Ctrl+ホイールは、`document` のキャプチャ段階で必ず
  `preventDefault()` する。** 外すとブラウザのページ拡大が発動し、背後の（画面より横に長い）
  ガント表ごと拡大されて、`position:fixed` のモーダルが画面の外へ逃げる。
  **詳細ビューの中だけに付けるのでは足りない**（枠の外やヘッダーの上で回したぶんが素通りする）
- **カレンダーは `calGrid`（画面に出ている7×n日）を基準に描く。**
  月で切り直すと月跨ぎが壊れる
- **コミットの著者は GitHub アカウント名義**（`itoshoji@users.noreply.github.com`）。
  個人のGmailは公開されていない。この状態を崩さないこと

## ⚠ 入り口の優先順位

**実際にガントチャートを使うのはPC（自宅・会社）がほとんど。** そこに最適化する。

1. **本番＝Googleドライブに置いた1ファイル版**（自宅PC・会社PC）。**ここが主役**
2. **GitHub＝バックアップと更新履歴**。設計の土台
3. **Vercel＝保留**。会社のフィルタで `vercel.app` が弾かれるため会社では使えない。
   **設計の中心に置かないこと**

**「まだ存在しないスマホ版のために、実際の使い方を歪めない」**（一度取り違えている。
`docs/経緯.md` 参照）

---

## 普段の手順

### 改良1回分（いちいち聞かないこと）

**本人はアプリの起動を全部Googleドライブから行う。** だから直しただけでは本人に届かない。
改良が終わったら、**確認を取らずにここまで自動でやる**:

```
python3 release.py -m "何を直したか"
```

＝ コミット（PCに保存）→ 1ファイル版をビルド → **ドライブに置く**。ここまでで1セット。

**GitHubへのpushはここでしない。** 本人が実際に触って感触が良ければ、そこで初めて
`git push`（＝バックアップ）。

```
直す → release.py（ドライブまで自動）→ 本人が触る → 良ければ git push
```

- **「ドライブに置きますか？」と聞かない**
- **push は勝手にやらない**
- ドキュメントだけの変更など、アプリの中身が変わらないときは版を出さない

### 作業を始めるとき

```
git fetch origin
git status -sb        # ahead/behind が出ないこと
```

**「ローカルにしか無い変更」を見落としたまま作業を始めると事故る**（実際に起きている）。

1. `requirements.md` で仕様を確認する
2. 実装する
3. **`python3 test.py` を通す。** 落ちたまま版を出さない
4. 直した所にテストが無ければ**書き足す**（日付計算・段組み・祝日・色まわりは特にバグりやすい）
5. **ブラウザで実際に操作して目視確認する**（見た目とマウス操作はテストで担保できない）
6. 仕様を変えたら `requirements.md` も更新する

### セッションの起動

```
gantt        # 前回の続きから再開（~/.zshrc にエイリアス登録済み）
gantt-new    # 新しい話として始める
gantt-pick   # 過去のセッション一覧から選ぶ
```

デスクトップの **`スケジュール管理アプリ.command`** をダブルクリックしても同じ
（`gantt` と同じ動き）。

**`cc` は使わない**（Mac標準のCコンパイラと衝突）。**`bypassPermissions` は使わない**
（危険な操作の歯止めが無くなる）。

---

## 構成

ビルド不要。npm も Node.js も使わない。`index.html` をダブルクリックすれば動く。

| ファイル | 役割 |
|---|---|
| `index.html` | 画面の骨組み |
| `styles.css` | 見た目 |
| `config.js` | Supabaseの接続情報、ログイン用のメールアドレス |
| `auth.js` | ログイン |
| `holidays.js` | 日本の祝日をオフライン計算 |
| `store.js` | **データ層**。DBに触るのはこのファイルだけ |
| `app.js` | 画面の描画と操作 |
| `build_standalone.py` | 1ファイル版を作る |
| `release.py` | コミット→ビルド→ドライブ配置をまとめて行う |
| `test.py` | テストをまとめて走らせる |
| `tests/` | テスト本体。`app.js` から実物のコードを切り出して動かす |

### 開発環境の制約

このMacには **Node.js も Homebrew も入っていない**。勝手に入れない。代わりに:

- **ロジックのテスト**: macOS標準の JavaScriptCore。まとめて走らせるには
  ```
  python3 test.py
  ```
  テストを書くときの決まりごと:
  - **`app.js` から実物の関数を切り出して（`grabFn`）動かすこと。**
    テスト側にコードを写して置くと、直したときにテストだけ古いまま残る
  - **ファイル名は英数字にする。** jsc は日本語のファイル名を開けない
  - `const` はevalのスコープ外に漏れないので、`grabConst` で `var` に読み替えて渡す
  - DOMが要るものは偽物を渡す（`tests/drag.js` が例。偽のバーを渡して
    実物の `startTaskDrag` をそのまま動かしている）
  - **書いたら一度わざと壊して、ちゃんと落ちることを確かめる。**
    通ったまま何も守っていないテストが一番たちが悪い
- **ブラウザでの確認**: Chrome拡張は `file://` を開けないので、ローカル配信してから見る
  ```
  python3 -m http.server 8765 --bind 127.0.0.1 --directory /Users/shoji/projects/gantt-app
  ```
  `--directory` を必ず付ける（付けないとホームフォルダ全体を配信してしまう）
- CSSやJSを直した直後はキャッシュが残ることがある。強制リロード（Cmd+Shift+R）する

---

## 設計の要点

### store.js

- 起動時に `Store.init()` で全件読み込み、メモリ上の `state` に載せる
- 参照系は**同期**。だから app.js は書き換え不要で、画面の反応も待ち時間ゼロ
- 更新系は**楽観更新**。先にメモリを書き換えて即描画し、通信は裏で直列に流す。
  失敗したら `Store.onError` → 画面上部に赤い帯
- DBはスネークケース、アプリはキャメルケース。**変換は store.js の中だけで完結させる**
- テーブルは `groups` / `tasks` / `subtasks`。`ON DELETE CASCADE` で親を消せば子も消える
- localStorage時代のデータは初回起動時に自動で引っ越す
  （`gantt-app:migrated-to-supabase` で二重実行を防止）

### Undo

**「逆操作」ではなく「丸ごと控えて差分同期」。** `History.act(ラベル, fn)` で操作を包むと、
前後の状態を比べて履歴に積む。`Store.restore()` が差分を出してSupabaseへ流す
（作成は親→子、削除は子→親。外部キーがあるため）。

### 見た目

**ガントもカレンダーも「淡い下地＋左端に色の帯＋濃い文字」で統一している。**
色の面積を減らすのが要点（`docs/経緯.md` 参照）。

- 項目の色は**赤・青の2色のみ**。中タスクは20色から選べる（`tasks.color`、NULLなら親の色）
- カレンダーの予定バーは**2行**（上＝項目名、下＝予定名）。中タスク名は出さない
- **「＋2件」のようにまとめて隠す方式は使わない**（本人が明確に拒否）
- ガント／カレンダー／「日」表示の3か所とも、**マウスを乗せると全文が小窓で出る**
  （`showTip`）。ブラウザ標準の `title` は遅くて見た目も揃わないので使わない
- 寸法は `styles.css` と app.js の**両方に書いてあるので必ず揃える**。
  `--cal-bar-h` / `--cal-bar-gap` ↔ `CAL_BAR_H` / `CAL_BAR_GAP`、
  `--dv-sub-h` / `--dv-sub-gap` ↔ `DV_SUB_H` / `DV_SUB_GAP`
- ガントのセルは `data-month` を持つ。選択の見た目は `paintSelection()` が
  描画のたびに当て直す（一覧・カレンダー・吹き出し・詳細ビューの4か所を同時に塗る）

### 詳細ビュー

一覧が「項目×月の俯瞰」なら、こちらは**少数の項目の中身を組み立てる場**（`requirements.md` 4.7）。

- **横軸の単位がトグルで変わる。** 年・四半期は月のマス（通し月番号）、月・週は日のマス（通し日番号）。
  `dvAxis()` がその範囲を返し、`dvPlace()` が一覧の `applyBarGeometry()` と同じ計算を
  単位に依らない形でやる。**新しい表示を足すときは `dvAxis()` に足すだけで済むようにしてある**
- **縦は「中タスクごとのブロック」**。上にバー、下に点線の縄張り＋小タスク。
  中タスクを最上段にまとめる形にすると、期間が重なったとき点線が交差して読めなくなる。
  **この形を崩さないこと**
- **中タスクを動かせるのは年・四半期だけ。** 中タスクは月単位のものなので、
  日の目盛りの上でドラッグさせると1マスの意味が合わなくなる
- 月・週は年度をまたぐことがある。`dvTasksOf()` はかかる年度を両方見ている
- **横軸は「見えているぶん」ではなく年度まるごとを一続きで持つ**（`dvAxis()`）。
  月や週で切ると、月末から翌月頭にかかる予定が見えず月跨ぎを確かめられない。
  画面に見せる量は `DV_WINDOW` から1列の幅を割り出して決め、あとは横スクロール。
  **「月表示だから1ヶ月だけ描く」形に戻さないこと**
- **1列の幅は px で持つ**（`dvColW`。null なら画面幅に合わせる）。`1fr` をやめたのは、
  Ctrl+ホイールで拡大したときに横スクロールさせるため。`--dv-col-w` / `--dv-track-w` を
  `#detailInner` に載せて、目盛りと中身が同じ幅を見るようにしている
- **拡大のたびに `renderDetail()` を呼ばない。** `dvApplyColW()` で CSS 変数だけ
  差し替える。年度まるごとぶんのマス目を作り直すことになり、拡大がガクガクになる
- **左の見出し列を貼り付けるところは grid ではなく flex で組む。**
  grid のマス目は sticky の効く範囲もマス目までに縛るので、横に送ると
  見出し列がマス目ごと流れて見切れる（実際にそうなった）
- **`resize` での描き直しは必ず間引く。** 詳細ビューは丸ごと作り直すので、
  毎回描くと埋め込み表示などで画面が固まる（実際に固まった）
- **中タスクと小タスクは形で分ける**（角ばったバー＋上辺の帯 ／ 丸いピル＋色の点）。
  同じ描き方にすると上下に並んだとき見分けが付かない。**色を濃くして分けようとしないこと**
  （色の面積を減らす方針に反する）

### 操作体系

- ワンクリック=選択、**ダブルクリック=作成/展開**（カレンダーも同じ）
- ショートカットは `Cmd` と `Ctrl` の**両方**を受け付ける（`e.metaKey || e.ctrlKey`）
- 複製ドラッグだけOSごとに分ける → **Mac は Option(⌥)+ドラッグ、Windows は Ctrl+ドラッグ**。
  Macで `Ctrl+ドラッグ` は右クリック扱いになりコンテキストメニューと衝突する
- Undo は `Cmd+Z` / `Ctrl+Z` に加えて**右クリックメニューにも置く**（マウスだけで戻せるように）

---

## 開発体制

- ユーザーはアプリ開発が初心者。ターミナル・git・各種アカウント発行の概念に不慣れ。
  手順を案内するときは「どのボタンをどう押すか」まで具体的に書く
- 実装はClaude Code側がほぼ巻き取る。ユーザーの役割は「動作確認」「フィードバック」
  「アカウント作成など本人にしかできない操作」
- コミュニケーションは関西弁混じりのカジュアルな口調
- **多くの作業をiPhoneからリモート操作で行っている。** ただし操作対象はこのMacなので、
  作業中このMacは動いている
- **決定事項として書くのは、本人が実際にそう言ったことだけ。** 自分で出した結論を
  本人の判断であるかのように書かない（一度やらかしている。`docs/経緯.md` 参照）

---

## 外部サービス

| | |
|---|---|
| GitHub | https://github.com/itoshoji/gantt-app （Public） |
| Supabase | プロジェクト `gantt-app` / `https://gnokupdporpcvbivvibh.supabase.co` |
| Vercel | https://gantt-app-kohl.vercel.app （`main` にpushで自動反映） |

### Googleドライブでの版配布

会社PCでは `vercel.app` が弾かれるため、1ファイルに全部埋め込んだHTMLを置いて、
ダウンロードしてダブルクリックで使う。

```
~/Library/CloudStorage/GoogleDrive-<アカウント>/マイドライブ/claude開発アプリ/スケジュール管理/
    スケジュール管理_v7.html   ← 最新をここに置く
    過去の版/                  ← 古い版はここへ退ける
```

**このドライブが会社PCと自宅PCを繋いでいる。** 出先からリモートで直したものが、
そのまま翌日の会社PCで使える。だから改良のたびにここへ置くのが要になる。

**フォルダ名にGoogleアカウントのメールアドレスが入るが、このリポジトリはPublicなので
ドキュメントにもコードにも直書きしない。** `release.py` の `find_drive()` が
`GoogleDrive-*` を探して見つける。

**アプリのフォルダ直下に、HTMLをそのまま置く。** `cp` するだけで自動同期される。
版はファイル名の番号（`スケジュール管理_v7.html`）で見分け、同じ版を上書きせず番号を増やす。
**版ごとに `vN` フォルダを作る形に戻さないこと。**

`claude開発アプリ/` の下にはもう1つ `cat1集計/`（経理データ集計ツール）がある。
**ドライブ直下にファイルを散らかさず、アプリのフォルダに入れること。**

MCPの `create_file` でも上げられるが、100KB超を丸ごと送ることになり非常に効率が悪い。
**同期フォルダに置く方法を使うこと。**

**ファイルは「入り口」でしかなく、データはSupabaseにある。**
古い版のHTMLでも予定は消えない。差し替えれば済む。

### ログイン

- メールアドレスは `config.js` の `APP_LOGIN_EMAIL` で固定（`owner@example.com`）。
  画面ではパスワードだけを聞く
- トークンは localStorage、期限1分前に自動更新。**端末ごとに一度入れれば入りっぱなし**
- 401/403 は `err.isAuth` を立ててログイン画面へ戻す。ログアウト時は `Store.reset()`
- パスワードを忘れたら Supabase の Authentication → Users から再設定
  （アドレスが実在しないのでメールでの復旧はできない）

---

## つまずきポイント

### GitHubへの push 認証

`credential.helper` は `osxkeychain`。通常はClaude側から `git push` するだけでよい。
再設定が必要になったとき:

- トークンは **fine-grained personal access token**。`gantt-app` のみ /
  **Contents: Read and write**
- **`git push` の対話プロンプトはClaude側から扱えない**。本人のターミナルで実行してもらう
- **⚠ 最大のつまずき: パスワード入力時、貼り付けても画面に何も表示されない。**
  `●` すら出ない。「効いていない」と誤解して何度も貼り直す事故が起きた。
  **事前に「見た目が変わらなくても、そのままEnterを押してよい」と必ず伝えること**
- GitHubのログインパスワードでは通らない。必ずトークンを使う
- 失敗した情報が残っていると再試行も失敗する。先に消す:
  ```
  printf "protocol=https\nhost=github.com\n\n" | git credential-osxkeychain erase
  ```

### 自動モードでブロックされる操作

- `gh auth login`（`gh` はそもそもこのMacに入っていない）
- `git filter-branch` / `git rebase --root --exec`（履歴の書き換え全般）
- `git remote add` と `git fetch` を `&&` で繋いだ複合コマンド
  → **単体コマンドに分割すれば通る**

### 既知の制限

- **スマホのレイアウトが未対応。** ログインと基本の閲覧はできるが、タイトルが縦に潰れ、
  月見出しも詰まる。ダイアログだけは先に直してある（`.overlay` を `100vw / 100dvh` に固定。
  ガント表が横に長くページ幅が画面幅を超えるため、`inset:0` だけでは画面外にずれる）
- **Claude側からログイン後の画面を操作して検証できない**（パスワードを持たないため）。
  キー操作やドラッグの最終確認は本人に依頼すること

---

## 次にやること

1. **【最優先・未確認】会社PCでの動作確認**（2026-07-30予定）。
   ドライブから最新のHTMLを落としてダブルクリックで動くか。
   会社PCから supabase.co / github.com へのアクセスは確認済みで問題なし
2. **実際に使ってみての手直し。** 実運用で出る不満を優先する
3. **スマホ版**（本人の判断で保留中）。着手するときは「エンジンは共有し、画面だけ作る」方針。
   既存の「日」表示が原型として近い。まず「見るだけか、編集もするか」を本人に確認すること

### 白紙に戻したもの

- **仕事／プライベートの切り替え**（2026-07-31 廃止）。「プライベートはGoogleカレンダーで十分」
  との本人判断。トグル・`scope` の絞り込み・「プライベートへ移動」を削除した。
  **DBの `groups.scope` カラムだけは残してある**（消す得が無いため）。新規項目には `'work'` が入る
- **Googleカレンダー等からの家族の予定の取り込み**（2026-07-31 白紙）。
  プライベート用途をやめたので前提ごと消えた。**蒸し返さないこと**
