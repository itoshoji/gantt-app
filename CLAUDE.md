# gantt-app

ガントチャート型タスク管理Webアプリ。Excelで手作業管理していた月次ガント表を置き換えるのが目的。
仕様の正本は `requirements.md`（v3）。**作業を始める前に必ず読むこと。**

## 開発体制

- ユーザーはアプリ開発が初心者。ターミナル・git・各種アカウント発行の概念に不慣れ。
  手順を案内するときは「どのボタンをどう押すか」まで具体的に書く。
- 実装はClaude Code側がほぼ巻き取る前提。ユーザーの役割は「動作確認」「フィードバック」
  「アカウント作成など本人にしかできない操作」に限定するという合意になっている。
- コミュニケーションは関西弁混じりのカジュアルな口調。
- 多くの作業をスマートフォン（iPhone）からリモート操作で行っている。
  そのため、Mac側でウィンドウを開くような確認方法は役に立たないことがある。

## 構成

ビルド不要。npm も Node.js も使わない。`index.html` をダブルクリックすれば動く。

| ファイル | 役割 |
|---|---|
| `index.html` | 画面の骨組み |
| `styles.css` | 見た目 |
| `config.js` | Supabase の接続情報（URL と publishable key） |
| `holidays.js` | 日本の祝日をオフライン計算（春分秋分の近似式・ハッピーマンデー・振替休日・国民の休日） |
| `store.js` | **データ層**。Supabase に保存する。DBに触るのはこのファイルだけ |
| `app.js` | 画面の描画と操作 |

ES Modules は使わない（`file://` で開くと CORS で動かなくなるため）。
すべてクラシックスクリプトで、読み込み順は
**config → holidays → store → app**（後ろが前に依存しているので順番を変えないこと）。

## 開発環境の制約と回避策

このMacには **Node.js も Homebrew も入っていない**。勝手に入れない。代わりに：

- **ロジックのテスト**: macOS標準の JavaScriptCore を使う
  ```
  /System/Library/Frameworks/JavaScriptCore.framework/Versions/A/Helpers/jsc テスト.js
  ```
  `const` はevalのスコープ外に漏れないので、テスト対象とテスト本体は
  1回の `eval()` にまとめて渡すこと。
- **ブラウザでの動作確認**: Chrome拡張は `file://` を開けないので、ローカル配信してから見る
  ```
  python3 -m http.server 8765 --bind 127.0.0.1 --directory /Users/shoji/projects/gantt-app
  ```
  `--directory` を必ず付ける（付けないとホームフォルダ全体を配信してしまう）。
- CSSやJSを直した直後はブラウザにキャッシュが残ることがある。反映されないときは
  強制リロード（Cmd+Shift+R）するか、`fetch(url, {cache:'reload'})` してから reload する。

## 作業の進め方

1. `requirements.md` で仕様を確認する
2. 実装する
3. **ロジックは jsc でテストを書いて通す**（日付計算・段組み・祝日まわりは特にバグりやすい）
4. **ブラウザで実際に操作して目視確認する**（ドラッグ操作はテストで担保できない）
5. 仕様を変えたら `requirements.md` も更新する

## 現在の状態

v3まで実装済み。主な機能：

- 仕事 / プライベートの切り替えトグル。項目ごとに `scope` を持ち、選択中の側だけを
  ガント・カレンダー・日ビューに表示する（選択状態は localStorage に保持）
- 年 / 四半期 / 日 の表示トグル（選択状態は localStorage に保持）
- 項目（旧「大枠」）→ 中タスク（月単位）→ 小タスク（日単位）の3階層
- セルクリックで中タスク作成、バーのドラッグで移動・端で伸縮
- **中タスクを移動すると小タスクも同じ月数ずれる**（31日→30日に丸める）。
  **伸縮したときは、はみ出した小タスクだけ端の月に寄せる**
- カレンダーは日をまたぐひと繋がりのバーで描画。週ごとに段を詰める。
  予定が多くても画面に収まるようスクロールし、曜日見出しは上に固定
- 土曜=うっすら水色 / 日曜・祝日=うっすら赤
- 右クリックでコンテキストメニュー（名前を変更 / カレンダーで見る / 削除）
- 項目の非表示ボタン、ドラッグ＆ドロップ並べ替え

追加済み（2026-07-27）:

- 画面左上の表示名は「スケジュール管理」（アプリ名。リポジトリ名は `gantt-app` のまま）
- **仕事 / プライベートのスコープ切り替えトグル**。画面全体が連動して切り替わる

### GitHub

**https://github.com/itoshoji/gantt-app （Public）**

- Web UIのドラッグ＆ドロップでアップロード済み。`main` はローカルとリモートで同期済み
- `.claude/` は `.git/info/exclude` でローカル限定の無視指定にしてある
- コミットの著者は GitHub アカウント名義。**個人のGmailアドレスは公開されていない**ので、
  今後もこの状態を崩さないこと（ローカルの `user.email` は
  `itoshoji@users.noreply.github.com` に設定済み）

### Supabase（クラウド保存）

移行済み。プロジェクト `gantt-app` / URL `https://gnokupdporpcvbivvibh.supabase.co`。
接続情報は `store.js` の先頭に直書き（publishable key はフロントに置く前提のキー）。

テーブルは `groups` / `tasks` / `subtasks` の3つ。`ON DELETE CASCADE` で親を消せば子も消える。

**store.js の設計（ここを崩さないこと）**

- 起動時に `Store.init()` で全件読み込み、メモリ上の `state` に載せる
- 参照系は**同期**のまま。だから app.js は書き換え不要で、画面の反応も待ち時間ゼロ
- 更新系は**楽観更新**。先にメモリを書き換えて即描画し、通信は裏で直列に流す。
  失敗したら `Store.onError` → 画面上部に赤い帯を出す
- DBはスネークケース、アプリはキャメルケース。変換は store.js の中だけで完結させる

**⚠ 型の落とし穴**: `start_month` / `end_month` は **TEXT** カラムなので、DBからは
`"3"` のような文字列で返ってくる。app.js は月を 0〜11 の**数値**として大小比較するため、
読み込み時に `Number()` で戻すこと（`toTask()` で実施済み）。ここを外すと
バーの位置がずれる。将来 SQL で INTEGER に変えるなら、この変換は残したままでよい。

localStorage時代のデータは初回起動時に自動で引っ越す（`gantt-app:migrated-to-supabase`
で二重実行を防止）。

### ログイン（2026-07-28 導入済み）

**リポジトリが Public でも問題ない設計にしてある。** 守っているのは接続キーの秘匿ではなく
DB側の RLS（`FOR ALL TO authenticated`）。**ここを `allow all` に戻すと即座に穴が開く。**

- 利用者は本人ひとり。**メールアドレスは `config.js` の `APP_LOGIN_EMAIL` で固定**し、
  画面ではパスワードだけを聞く（入力の手間を最小にするため）。
  アドレスは `owner@example.com`。実在しない説明用の領域を使っているので、
  **本人のGmailをリポジトリに晒さずに済む**（一度Gmailで作られたが作り直した経緯あり）。
- トークンは localStorage に置き、期限1分前に自動更新。**端末ごとに一度入れれば入りっぱなし。**
- `Store` は毎回本人のアクセストークンを添えて送る。401/403 は `err.isAuth` を立てて
  ログイン画面へ戻す。ログアウト時は `Store.reset()` で手元のデータも消す。
- パスワードを忘れたら Supabase の Authentication → Users から再設定する
  （アドレスが実在しないのでメールでの復旧はできない）。

検証済み: ログインなしでの書き込みは 401（RLS違反）、読み取りは空配列。データは消えていない。

新しくテーブルを足したときは、**RLSを有効にして同じ `authenticated only` ポリシーを付けること。**
付け忘れるとその表だけ誰でも読み書きできる状態になる。

### （旧）ログインを一度撤去した経緯

一度 Supabase Auth のログイン画面まで実装したが、**本人の判断で全面的に撤去した**
（2026-07-27）。理由は「手帳に鍵をかけるほどのものではない、毎回の入力が煩わしい」。

一度は「手帳に鍵をかけるほどではない、毎回の入力が煩わしい」として全面撤去した。
その後 2026-07-28 に再検討して導入した。**決め手は「毎回ではなく端末ごとに一度だけ」**
という点が誤解されていたこと。撤去を再提案する必要はない。

新規サインアップは**停止済み**（`/auth/v1/settings` の `disable_signup: true` で確認）。
勝手にアカウントを作られて `authenticated` になられる経路は塞がっている。
**再び有効にしないこと。**

### 実装のポイント（2026-07-28 の追加分）

- **Undo は「逆操作」ではなく「丸ごと控えて差分同期」**。`History.act(ラベル, fn)` で
  操作を包むと、その前後の状態を比べて履歴に積む。`Store.restore()` が差分を出して
  Supabase に流す（作成は親→子、削除は子→親の順。外部キーがあるため）。
  **新しい更新操作を足すときは `History.act` で包むこと。** 包み忘れると戻せなくなる。
- 中タスクの色は `tasks.color`（NULL可）。NULLなら親の項目の色を使う（`taskColor()`）。
- カレンダーは月ではなく **`calGrid`（画面に出ている7×n日）** を基準に描く。
  前後の月のマスにも `data-date` を持たせてあるので、`dateAtPoint()` がそこも返す。
  **ここを月で切り直すと月跨ぎが壊れる。**
- セルは `data-month` を持つ。選択の見た目は `paintSelection()` が描画のたびに当て直す。

### 決定済みの仕様（2026-07-28、実装済み）

- **色**: 項目は赤・青の2色のみ。中タスクはカラーピッカーで自由な色
- **クリック体系**: ワンクリック=選択、**ダブルクリック=作成/展開**。カレンダー画面も
  ダブルクリックで統一する
- **ショートカット**: コピー等は `Cmd` と `Ctrl` の**両方**を受け付ける
  （`e.metaKey || e.ctrlKey`）。複製ドラッグだけはOSごとに分ける
  → **Macは Option(⌥)+ドラッグ、Windowsは Ctrl+ドラッグ**。
  Macで `Ctrl+ドラッグ` を使ってはいけない（右クリック扱いになりコンテキストメニューと衝突）
- **Undo**: `Cmd+Z` / `Ctrl+Z` に加えて、**右クリックメニューにも「元に戻す」を置く**
  （マウスだけで戻せるように）。切り取り・削除・移動・伸縮が対象
- **スマホ対応は先送り**。タッチ時の「長押しで選択」はスマホ版を作るときに検討する。
  それまでiPhoneからの編集はやりにくい状態を許容する

### デプロイ（2026-07-28 公開済み）

**https://gantt-app-kohl.vercel.app** — Vercel（Hobby / 無料枠）。

- GitHubの `main` に push すると**自動で反映される**。Vercel側の操作は不要
- ビルドなしの静的配信。`vercel.json` で `Cache-Control: max-age=0, must-revalidate` を
  付けている。**ファイル名にハッシュが付かない構成なので、これを外すと
  更新しても古いJS/CSSが使われ続ける**
- httpは308でhttpsへ転送される

検証済み: ログイン画面が出る／未ログインではデータが空／スマホ幅でもログインできる。

### スマホ表示の現状

**ログインと基本の閲覧はできるが、レイアウトは未対応。** タイトルが縦に潰れ、
月見出しも詰まる。本格対応は「スマホ版」として別途やる方針（2026-07-28 合意）。
ダイアログだけは先に直してある（`.overlay` を `100vw / 100dvh` に固定。
ガント表が横に長くページ幅が画面幅を超えるため、`inset:0` だけでは画面外にずれる）。

### 次にやること

当初の3ステップ（GitHub / Supabase / デプロイ）は**すべて完了**。
Macからの push も通るので、コードを直したら `git push` するだけで公開先まで反映される。

残っているのは:

1. **実際に使ってみての手直し**（機能を一気に7つ入れたので、使用感の調整が要るはず）
2. **スマホ版のレイアウト**（上記「スマホ表示の現状」を参照）
3. **Googleカレンダー等からの家族の予定の取り込み**（構想段階）。
   他人のデータを扱うことになるので、着手前に扱いを本人と相談すること

会社PCから vercel.app / supabase.co / github.com へのアクセスは確認済みで問題なし。

### 自動モードでブロックされた操作（回避策込み）

- `gh auth login`（CLI認証そのものが通らない）
- `git filter-branch` / `git rebase --root --exec`（履歴の書き換え全般）
- `git remote add` と `git fetch` を `&&` で繋いだ複合コマンド
  → **単体コマンドに分割すれば通る**

`gh` コマンドはそもそもこのMacに入っていない。

### 既知の制限

- カレンダー上で予定をドラッグするとき、**表示中の月の中にしか落とせない**。
  月をまたいで動かしたい場合は ‹ › で月を送る必要がある。
- Macからの push は未設定（上記「次にやること」1を参照）。
